const fs = require('fs');

let file = fs.readFileSync('app/page.tsx','utf8');

file += `
<section style="margin-top:60px;text-align:center">
  <h2>Start building with OVWI</h2>
  
  <div style="margin-top:20px">
    <button onclick="window.location.href='/auth/login'" class="verify-btnwriteFileSync('app/page.tsx', file);
